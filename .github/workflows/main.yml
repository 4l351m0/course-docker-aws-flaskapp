name: CI Basico

on: push

jobs:
 simple_job:
  runs-on: ubuntu-latest

  steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Run a simple command
      run: echo "¡Hola desde mi primer workflow de GitHub Actions!"